import React from "react";

const CheckboxField = (props) => {
  return (
    <div>
      <label> </label>
      <input type="checkbox" className={props.className}></input>
    </div>
  );
};

export default CheckboxField;
